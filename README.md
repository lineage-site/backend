# backend

# Heroku Setup
```sh
brew tap heroku/brew
brew install heroku
heroku login
```
## Allow direnv to manage heroku team
```sh
brew install direnv
direnv allow
```

# Python Setup
```sh
brew install pipenv
pipenv install
```
